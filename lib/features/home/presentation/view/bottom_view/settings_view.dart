import 'package:flutter/material.dart';

class Settingsiew extends StatelessWidget {
  const Settingsiew({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Settings',
        style: TextStyle(fontSize: 30),
      ),
    );
  }
}
